// src/components/ui/SelectField/SelectField.css.ts
import { style } from '@vanilla-extract/css';
import { baseSelect } from '../../../styles/layout.css.ts';

export const select = style([baseSelect]);

export const wrapper = style({
  width: '100%',
  position: 'relative',
});